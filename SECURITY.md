# 安全政策

## 支持范围

ImageViewer 尚无稳定版本或官方发行包。当前仅评估默认分支最新代码中的安全问题，不承诺旧提交、分支或 `app/` 历史原型的安全更新时限。

## 私下报告漏洞

请优先使用代码托管平台的 **Private vulnerability reporting（私密漏洞报告）** 功能联系仓库所有者。若页面没有该入口，请联系仓库所有者请求一个私密报告渠道；**不要在公开 Issue、讨论区或 PR 中披露漏洞细节，也不要附带真实私人媒体。**

报告应包含：

- 受影响提交、平台和 Electron/Node 版本；
- 可复现的最小步骤与预期/实际结果；
- 影响范围、可利用条件及建议缓解方式；
- 仅使用无敏感内容的演示文件。

维护者确认收到后会协商披露与修复计划。由于项目处于志愿维护的早期阶段，目前不保证固定响应时限或漏洞奖励。若无法建立私密渠道，请只公开请求联系方式，不要公开技术细节。

## 使用者注意事项

项目会读取用户选择的本地目录，并可调用系统程序打开文件或目录。请只扫描可信内容，及时更新依赖，不要以高权限运行，也不要将偏好数据或本地路径当作可安全公开的信息。
